import { createStore } from 'vuex'
import {userStore} from "./userStore"
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    userStore:userStore,
  }
})
